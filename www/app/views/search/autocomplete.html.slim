- if @movies.any?
  - @movies.each do |movie|
    li.movie-list-item[
        role="option"
        data-autocomplete-value= movie_path_for(movie)
      ]
      = render 'movies/list_item', movie: movie

  li.movie-list-item[
    role="option"
    aria-disabled="true"
    data-action="click->search-autocomplete#showAll"
    data-search-autocomplete-url-param=new_search_path(query: @query)
  ] Show all results

- else
  li.movie-list-item role="option" aria-disabled="true" There's nothing.
